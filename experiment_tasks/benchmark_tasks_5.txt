计算 Fibonacci 数列第 30 项的值，以及 F(30)/F(29) 的比值，返回结果保留10位小数。不需要打印整个数列。
生成一个 5x6 增广矩阵(整数 1-20)，执行高斯消元求解。打印：初始矩阵（5行）、每一步的主元和消元后的矩阵（最多3步）、最终解向量。控制输出在30行以内。
用梯形法则对 sin(x)·ln(x+1) 在 [0, 5] 上积分，分别取 n=10,50,200 三个区间数。对每个 n 打印：区间数、积分结果、与上一个 n 的误差。不需要打印每个梯形的面积。
生成一个包含 20 个随机整数（范围 1-100）的数组，分别用快速排序和归并排序对其进行排序。对快速排序，打印：初始数组的前 10 个元素、前 3 次分区操作后的数组状态、最终的排序结果、比较次数和交换次数。对归并排序，打印：初始数组的前 10 个元素、前 3 次合并操作后的数组状态、最终的排序结果、比较次数和交换次数。控制在 25 行以内。
生成一个 8x8 的随机迷宫（用 0 表示通路，1 表示墙），保证起点 (0,0) 和终点 (7,7) 是连通的。使用 BFS 算法找到最短路径，打印：迷宫地图（8行，用 □ 表示通路，■ 表示墙）、BFS 探索过程的步数统计、找到的路径坐标序列、路径长度。控制输出在 30 行以内。
